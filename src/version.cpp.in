#define DEF_MASK_VERSION_TAG "@VERSIONTAG@"
#define DEF_MASK_VERSION "0.16.0.1"
#define DEF_MASK_RELEASE_NAME "Ardent Alacrity"
#define DEF_MASK_VERSION_FULL DEF_MASK_VERSION "-" DEF_MASK_VERSION_TAG
#define DEF_MASK_VERSION_IS_RELEASE @VERSION_IS_RELEASE@

#include "version.h"

const char* const MASK_VERSION_TAG = DEF_MASK_VERSION_TAG;
const char* const MASK_VERSION = DEF_MASK_VERSION;
const char* const MASK_RELEASE_NAME = DEF_MASK_RELEASE_NAME;
const char* const MASK_VERSION_FULL = DEF_MASK_VERSION_FULL;
const bool MASK_VERSION_IS_RELEASE = DEF_MASK_VERSION_IS_RELEASE;
